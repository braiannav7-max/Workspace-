# Código de acceso al panel (requerido para entrar).
VITE_ADMIN_CODE=cambiame

# Supabase (OPCIONAL). Si los dejás vacíos, el panel corre en "modo local" con datos demo.
VITE_SUPABASE_URL=
VITE_SUPABASE_PUBLISHABLE_KEY=
